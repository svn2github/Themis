#include <Application.h>
#include <Window.h>

/*Ton application qui dérive de BApplication (dans l'Application Kit ! ;-)) )
*/
class TazApplication : public BApplication
{
	public:
				TazApplication();
		virtual	~TazApplication();
};
